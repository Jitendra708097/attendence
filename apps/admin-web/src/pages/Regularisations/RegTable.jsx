/**
 * @module RegTable  * @description Regularisation requests table.  *///
  import { Table, Space, Button, Tag, Popconfirm } from 'antd'; import { CheckOutlined, CloseOutlined, EyeOutlined } from '@ant-design/icons'; import StatusBadge from '../../components/common/StatusBadge.jsx';  export default function RegTable({ data, loading, onView, onApprove, onReject, pagination }) {   const columns = [     { title: 'Employee', dataIndex: 'employeeName', key: 'employeeName' },     { title: 'Date', dataIndex: 'date', key: 'date' },     { title: 'Type', dataIndex: 'type', key: 'type', render: (type) => <Tag>{type}</Tag> },     { title: 'Requested Times', dataIndex: 'requestedTimes', key: 'requestedTimes' },     {       title: 'Status',       dataIndex: 'status',       key: 'status',       render: (status) => <StatusBadge status={status} />,     },     { title: 'Level', dataIndex: 'level', key: 'level' },     {       title: 'Actions',       key: 'actions',       render: (_, record) => (         <Space>           <Button type= "text " icon={<EyeOutlined />} size= "small " onClick={() => onView(record)} />           {record.status === 'pending' && (             <>               <Popconfirm                 title= "Approve regularisation? "                 onConfirm={() => onApprove(record.id)}                 okText= "Yes "                 cancelText= "No "               >                 <Button type= "text " icon={<CheckOutlined />} size= "small " />               </Popconfirm>               <Button                 type= "text "                 danger                 icon={<CloseOutlined />}                 size= "small "                 onClick={() => onReject(record.id)}               />             </>           )}         </Space>       ),     },   ];    return (     <Table columns={columns} dataSource={data} loading={loading} rowKey= "id " pagination={pagination} scroll={{ x: 1000 }} />   ); } 