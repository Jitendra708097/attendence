/**
 * @module DepartmentTree  * @description Department hierarchy tree.  *///
  import { Tree, Button, Space } from 'antd'; import { EditOutlined, DeleteOutlined, PlusOutlined } from '@ant-design/icons';  export default function DepartmentTree({ data, onAdd, onEdit, onDelete }) {   const treeData = data?.map((dept) => ({     title: (       <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', width: '100%' }}>         <span>           {dept.name} ({dept.employeeCount} employees)         </span>         <Space onClick={(e) => e.stopPropagation()}>           <Button type= "text " size= "small " icon={<PlusOutlined />} onClick={() => onAdd(dept.id)} />           <Button type= "text " size= "small " icon={<EditOutlined />} onClick={() => onEdit(dept)} />           <Button type= "text " size= "small " danger icon={<DeleteOutlined />} onClick={() => onDelete(dept.id)} />         </Space>       </div>     ),     key: dept.id,     children: dept.children || [],   })) || [];    return <Tree treeData={treeData} defaultExpandAll />; } 