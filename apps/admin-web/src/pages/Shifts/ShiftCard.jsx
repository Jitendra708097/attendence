/**
 @module ShiftCard  * @description Shift info card.  *///
 import { Card, Button, Space, Tag, Statistic, Row, Col } from 'antd'; import { EditOutlined, DeleteOutlined } from '@ant-design/icons';  const DAYS = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];  export default function ShiftCard({ shift, onEdit, onDelete }) {   const workDays = DAYS.filter((_, i) => shift.workDays?.includes(i));    return (     <Card       title={shift.name}       extra={         <Space>           <Button type= "text " icon={<EditOutlined />} onClick={() => onEdit(shift)} />           <Button type= "text " danger icon={<DeleteOutlined />} onClick={() => onDelete(shift.id)} />         </Space>       }       style={{ height: '100%' }}     >       <p>         <strong>Time:</strong> {shift.startTime} - {shift.endTime}       </p>       <p>         <strong>Work Days:</strong> {workDays.join(', ')}       </p>        <Row gutter={[16, 16]} style={{ marginTop: 16 }}>         <Col xs={12}>           <Statistic title= "Grace (in) " value={shift.graceCheckIn} suffix= "min " />         </Col>         <Col xs={12}>           <Statistic title= "Grace (out) " value={shift.graceCheckOut} suffix= "min " />         </Col>         <Col xs={12}>           <Statistic title= "Half Day " value={shift.halfDay} suffix= "min " />         </Col>         <Col xs={12}>           <Statistic title= "Absent " value={shift.absent} suffix= "min " />         </Col>         <Col xs={12}>           <Statistic title= "OT After " value={shift.otAfter} suffix= "min " />         </Col>         <Col xs={12}>           <Statistic title= "Employees " value={shift.employeeCount || 0} />         </Col>       </Row>     </Card>   ); } 